services:
  travel-booking-public-development:
    build:
      context: ../../
      dockerfile: docker/Dockerfile
    image: travel-booking-public-development:latest
    ports:
      - "3000:3000"
    env_file:
      - ../../.env